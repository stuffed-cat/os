//! Deprecated keyboard helpers (previously used by an in-kernel shell).

#![allow(dead_code)]

/// Placeholder type retained temporarily to avoid breaking tree history.
pub struct RemovedKeyboardSupport;
