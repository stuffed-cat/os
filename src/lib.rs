//! Workspace-level crate stub.
//! This crate only exists to host metadata used by tooling such as
//! `cargo bootimage`, and is not intended to build any artifacts.

#![allow(clippy::all)]
#![allow(dead_code)]

/// Marker type to keep the crate non-empty.
pub struct WorkspaceMetadata;
